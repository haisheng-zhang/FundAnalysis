# 设计说明

## 核心架构

项目采用前后端分离架构：

- **后端 (Backend)**: 基于 Python FastAPI，负责数据抓取、处理和实时行情缓存。
- **前端 (Frontend)**: 基于 Next.js，负责展示基金持仓及估算涨幅。

## 技术栈

| 部分 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Python (FastAPI) | 高性能异步 Web 框架 |
| 数据源 | akshare | 实时股票行情及基金持仓数据 |
| 数据处理 | pandas | 高效的矩阵运算和数据清洗 |
| 前端框架 | Next.js (React) | 现代化的前端开发框架 |
| 样式 | Tailwind CSS | 快速构建响应式 UI |

## 关键设计

### 1. 实时行情缓存
- **机制**: 后端启动时开启一个独立的后台线程。
- **频率**: 每 10 秒调用一次 `akshare` 获取全 A 股实时行情。
- **存储**: 使用应用内内存 (`_SPOT_CACHE` 变量)，不依赖外部数据库，保证极高的读取速度。
- **交易时间感知**: 仅在 A 股交易时段（9:30-11:30, 13:00-15:00）进行刷新。

### 2. 基金涨幅估算逻辑
- **数据获取**: 根据输入的基金代码，实时抓取该基金最新的季报前十大重仓股。
- **计算公式**: `基金估算涨幅 = Σ (重仓股涨跌幅 * 该股占净值比例) / Σ (前十大重仓股占净值比例) * (前十大重仓股总占比)`
  - *注：这是一种近似估算法，未考虑剩余持仓及盘中调仓。*

### 3. 前后端通信
- 前端通过环境变量 `NEXT_PUBLIC_API_URL` 获取后端地址。
- 调用接口：`GET /api/fund/{code}`。
- 支持跨域 (CORS) 请求。

### 4. 数据库设计 (未来扩展)
- **存放位置**: 如果后续引入持久化存储（如 SQLite 或 Supabase PostgreSQL），数据库文件应统一存放在 `backend/db/` 目录下（如果是云端 DB 则配置连接字符串），以保持前后端代码的物理隔离。
- **当前状态**: 目前所有数据均在内存中，无需物理数据库文件。

### 5. 支付集成预留 (Lemon Squeezy)
- **场景**: 如果需要收费，可以使用 Lemon Squeezy 提供的 Webhook。
- **逻辑**: 
  - 前端发起支付请求 -> 跳转 Lemon Squeezy -> 支付成功。
  - Lemon Squeezy 发送 Webhook 到后端 `/api/webhook/lemon`。
  - 后端更新数据库中用户的订阅状态。

## 目录结构

```text
fund/
├── backend/            # 后端 Python 代码
│   ├── api.py          # FastAPI 入口
│   ├── fund_estimation.py # 核心计算逻辑
│   └── requirements.txt # 依赖列表
├── frontend/           # 前端 Next.js 代码
│   ├── app/            # 页面与布局
│   └── ...
├── README.md           # 项目总览
├── DESIGN.md           # 设计文档
└── DEPLOY.md           # 部署指南
```
